{
    "name": "api",
    "root": "apps/backend/src/App",
    "projectType": "application",
    "targets": {
        "serve": {
            "executor": "@nx-dotnet/core:serve",
            "options": {
                "launch-profile": "https",
                "verbosity": "quiet"
            }
        }
    }
}